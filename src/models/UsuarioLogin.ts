export default interface UsuarioLogin {
    id: number; // está em falta
    nome: string; // está em falta
    usuario: string;
    senha: string;
    foto: string; // está em falta
    token: string; // está em falta
}